# Copy this file to `.env` and paste in your real keys.
# `.env` is gitignored — never commit your real keys.

# Groq — get one free at https://console.groq.com/keys
GROQ_API_KEY=

# Tavily — get one free at https://app.tavily.com/home
TAVILY_API_KEY=
